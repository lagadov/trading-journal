# Copy to .env.local and enter your new Massive key there. Never commit a real key.
# Massive Basic uses previous-trading-day closing prices, not live prices.
# Server-only variable: do not add a VITE_ prefix.
MASSIVE_API_KEY=
